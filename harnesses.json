{
  "effortRequiresModel": true,
  "harnesses": [
    {
      "name": "Claude Code",
      "id": "claude",
      "binary": "claude",
      "status": "supported",
      "controlWake": "background",
      "trust": "dialog",
      "discovery": {
        "models": "claude -p \"/model\" --output-format json",
        "effort": "claude -p \"/effort\" --output-format json",
        "answeredLocally": true
      },
      "modelFlag": true,
      "effortFlag": true,
      "permissionDefault": "--dangerously-skip-permissions",
      "forbiddenHeadless": [
        "claude -p"
      ],
      "instructionsFile": {
        "file": "CLAUDE.md",
        "readsAgentsMd": false,
        "needsImport": true,
        "importLine": "@AGENTS.md"
      },
      "notes": "worker-start --model pin honoured. The two discovery probes are answered locally (num_turns 0, total_cost_usd 0, no model turn); do not treat them as a dispatch. Trust is a dialog. A fresh repository shows the trust dialog even with --dangerously-skip-permissions, with No, exit preselected, reproduced 2026-09-11 on Claude Code 2.1.268; a worktree of an already-trusted repository inherits trust and shows none, which is what the withdrawn 2026-09-04 probe actually measured — do not re-propose that 2026-09-04 reading. Claude Code 2.1.273 and 2.1.274 word the dialog 'Quick safety check: Is this a project you created or one you trust?' with 'No, exit' preselected, so Enter refuses and exits the harness. Orca's prompt delivery sends Enter into that dialog, which is why a NO_ACK or PREFLIGHT FAIL quote on an untrusted path ends at a shell prompt and may hold only the tail 'Security guide' within the helper's 400-character quote (measured 2026-09-16 and 2026-09-17)."
    },
    {
      "name": "Codex CLI",
      "id": "codex",
      "binary": "codex",
      "status": "supported",
      "controlWake": "waker",
      "trust": "dialog",
      "discovery": {
        "models": "codex debug models",
        "effort": null,
        "omitVisibility": "hide",
        "effortFrom": "supported_reasoning_levels"
      },
      "modelFlag": true,
      "effortFlag": true,
      "permissionDefault": "--dangerously-bypass-approvals-and-sandbox",
      "forbiddenHeadless": [
        "codex exec"
      ],
      "instructionsFile": {
        "file": "AGENTS.md",
        "readsAgentsMd": true,
        "needsImport": false,
        "inert": [
          "CLAUDE.md"
        ]
      },
      "notes": "Codex CLI 0.154.0 shows its own trust dialog on a fresh directory ('Do you trust the contents of this directory?'), measured 2026-09-16 during the 0.19.0 checklist; trust is dialog, not orca-preflight. Control wakes by waker. Discovery: slugs with visibility hide are not offered; effort levels are supported_reasoning_levels on each slug, not one vocabulary per harness. Native AGENTS.md reader; CLAUDE.md is inert (not imported). '› 1. Yes, continue' is preselected, so Enter grants trust (measured 2026-09-16 on 0.154.0)."
    },
    {
      "name": "Grok Build",
      "id": "grok",
      "binary": "grok",
      "status": "deferred",
      "controlWake": "waker",
      "trust": "dialog",
      "discovery": null,
      "modelFlag": false,
      "effortFlag": false,
      "permissionDefault": "--permission-mode bypassPermissions",
      "forbiddenHeadless": [
        "grok --prompt-file"
      ],
      "instructionsFile": {
        "file": "AGENTS.md",
        "readsAgentsMd": true,
        "needsImport": false
      },
      "notes": "Deferred. Control wakes by waker. Grok Build 1.0.30 asks a y/n security question in a fresh repository (measured 2026-09-14), so trust is dialog, not none. Opens at the composer with no workspace-trust surface (measured 2026-08-29; unconfirmed by the 2026-09-11 method that retracted the Claude result — re-measure before relying on it). Grok validates root plugin.json (name and description only). worker-start --model does not support this id; models come from Orca's per-agent default arguments. PATH agent collides with this binary, so Cursor must not be documented as agent. No discovery command is offered while deferred."
    },
    {
      "name": "Antigravity CLI",
      "id": "antigravity",
      "binary": "agy",
      "status": "deferred",
      "controlWake": "waker",
      "trust": "dialog",
      "discovery": null,
      "modelFlag": false,
      "effortFlag": false,
      "permissionDefault": "--dangerously-skip-permissions",
      "forbiddenHeadless": [
        "agy -p",
        "agy --print"
      ],
      "instructionsFile": {
        "file": "AGENTS.md",
        "readsAgentsMd": true,
        "needsImport": false,
        "doNotOffer": [
          "GEMINI.md"
        ]
      },
      "notes": "Deferred. worker-start --agent agy returns agent_unconfigured; the id that resolves is antigravity. Control wakes by waker. 0.18.0 launched via an adopt path after output quiescence because a plain worker-start typed the prompt before the TUI was ready (0 of 4 on 2026-09-11); 0.19.0 deleted that path, so Antigravity workers no longer launch. Discovery while supported was agy models (TSV, offer the slug column) and effort low|medium|high from CLI help; some slugs already end in -high/-medium/-low — do not strip those suffixes. Native AGENTS.md; do not offer GEMINI.md. Root plugin.json is the agy package (name and description only; additionalProperties false). Install is agy plugin install of the git URL or a local path; the CLI has no plugin update. Do not write ~/.gemini. worker-start --model does not support this id; models come from Orca's per-agent default arguments."
    },
    {
      "name": "Kiro CLI",
      "id": "kiro",
      "binary": "kiro-cli",
      "status": "deferred",
      "controlWake": "unsupported",
      "trust": "dialog",
      "discovery": null,
      "modelFlag": false,
      "effortFlag": false,
      "permissionDefault": "none",
      "forbiddenHeadless": [
        "kiro-cli chat --no-interactive"
      ],
      "instructionsFile": {
        "file": "AGENTS.md",
        "readsAgentsMd": true,
        "needsImport": false
      },
      "notes": "Deferred. worker-start --agent kiro-cli returns agent_unconfigured; the id that resolves is kiro. Control wake is unsupported. permissionDefault is none; --trust-all-tools is forbidden on the launch argv because it opens a confirmation whose default is No, exit, and Orca's Enter kills the session. Once the TUI is idle, /tools trust-all was observed to trust tools for the session with no confirmation dialog. Headless kiro-cli chat --no-interactive is not a Dely worker. Native AGENTS.md. Discovery while supported was kiro-cli chat --list-models --format json (offer each model_id) and effort from kiro-cli chat --help; live 2.16.2 may offer only auto. Install is npx skills targeting the kiro-cli agent at global scope. No workspace-trust surface (measured 2026-08-29; unconfirmed by the 2026-09-11 method that retracted the Claude result — re-measure before relying on it). Do not write ~/.kiro or create custom agents. worker-start --model does not support this id; models come from Orca's per-agent default arguments."
    },
    {
      "name": "Cursor Agent CLI",
      "id": "cursor",
      "binary": "cursor-agent",
      "status": "supported",
      "controlWake": "background",
      "trust": "dialog",
      "discovery": {
        "models": "cursor-agent models",
        "effort": null,
        "modelSlugBefore": " - "
      },
      "modelFlag": true,
      "effortFlag": false,
      "permissionDefault": "--force",
      "forbiddenHeadless": [
        "cursor-agent -p",
        "cursor-agent --print"
      ],
      "instructionsFile": {
        "file": "AGENTS.md",
        "readsAgentsMd": true,
        "needsImport": false,
        "alsoApplies": [
          "CLAUDE.md"
        ]
      },
      "notes": "worker-start --model pin honoured; omit --effort (there is no effort flag; write the literal default). worker-start --agent cursor-agent returns agent_unconfigured; the id that resolves is cursor. Offer the model slug before ' - '. Do not invent an effort vocabulary, strip effort suffixes from slugs, or synthesize parameterized [effort=…] forms. Native AGENTS.md reader; the CLI also applies CLAUDE.md as a rule, so that file is not inert, but the import write offer stays on entries with needsImport. Never PATH agent (collides with Grok). Never -w/--worktree. Do not put --trust or an unpinned --sandbox on the launch argv. Trust dialog on a fresh directory, measured 2026-09-16 and 2026-09-17 on Cursor Agent 2026.09.10-fd3934a and 2026.09.15-d2fe57e: '⚠ Workspace Trust Required … Do you trust the contents of this directory?' with '▶ [a] Trust this workspace' preselected over '[q] Quit', so `a` or Enter grants trust, and the screen shows '⏳ Trusting workspace...' before the composer. Text sent in that state was dropped while `orca terminal send` returned ok; wait for the composer before sending a prompt."
    },
    {
      "name": "GitHub Copilot CLI",
      "id": "copilot",
      "binary": "copilot",
      "status": "deferred",
      "controlWake": "background",
      "trust": "dialog",
      "discovery": null,
      "modelFlag": false,
      "effortFlag": false,
      "permissionDefault": "--allow-all",
      "forbiddenHeadless": [
        "copilot -p",
        "copilot --prompt"
      ],
      "instructionsFile": {
        "file": "AGENTS.md",
        "readsAgentsMd": true,
        "needsImport": false,
        "alsoApplies": [
          "CLAUDE.md"
        ]
      },
      "notes": "Deferred. Control wakes by background. --allow-all is the documented equivalent of --allow-all-tools --allow-all-paths --allow-all-urls, with --yolo as its alias. No non-interactive model listing: copilot models fails with Invalid command format; copilot -p \"/model\" dispatched a real turn (auto-routed to claude-haiku-4.5). An unavailable --model value prints Using \"auto\" instead and continues. Effort while supported was from copilot --help: none|minimal|low|medium|high|xhigh|max. Native AGENTS.md and CLAUDE.md reader (--no-custom-instructions disables both). Trust: Confirm folder trust still opens with --allow-all (1. Yes preselected; never 2, which remembers the folder). Restore interrupted sessions then lists sessions from other folders; Esc starts fresh, Enter is a wrong-session hazard. Measured 2026-08-31 on Copilot CLI 1.0.82: Orca documented no copilot launcher id, so Control composed copilot --allow-all --effort high by hand. Install uses existing manifests into ~/.copilot/installed-plugins/dely/dely/; no sidecar. worker-start --model does not support this id; models come from Orca's per-agent default arguments."
    }
  ]
}
